[package]
name = "rns-embedded-core"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
serde = { workspace = true, features = ["derive"], optional = true }

[features]
default = ["std"]
std = []
alloc = []
serde = ["dep:serde"]

[lints]
workspace = true
