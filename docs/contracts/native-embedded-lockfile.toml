# Native embedded rollout lockfile.
# Checked by: cargo run -p xtask -- embedded-native-lock-check

contract_ble_camera_wire_ref = "366c35c"
contract_ble_transport_runtime_ref = "366c35c"
contract_native_embedded_interop_ref = "366c35c"
contract_native_embedded_lab_profile_ref = "phase0"
contract_native_embedded_node_config_ref = "phase1"

firmware_repo = "github.com/FreeTAKTeam/lxmf-embedded-fw"
firmware_ref = "5db94fe"
owners = ["@FreeTAKTeam/lxmf-maintainers"]

ci_workflow = ".github/workflows/nightly-embedded-hil.yml"
xtask_gate = "embedded-native-lock-check"
release_revision_mode = "pinned"
tcp_read_timeout_secs = 8
tcp_heartbeat_interval_ms = 30000
tcp_reconnect_backoff_ms = [1000, 2000, 5000, 10000, 30000]
capture_default_max_bytes = 1048576
capture_hard_max_bytes = 2097152
