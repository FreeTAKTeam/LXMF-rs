# v0.9.8 Release Candidate Evidence

This ledger records the exact evidence for the v0.9.8 release train. It is
updated as local, hosted, RC, and final publication gates complete.

## Candidate identity

- Candidate base SHA: `9f12fb4eafc09c7093e3a7f4132257572ada3aef`
- Candidate release SHA: `ba816e02f1af7650cbc19a1446d4486945dba690`
- Interop blocker-fix SHA: `ba816e02f1af7650cbc19a1446d4486945dba690`
- RC tag: pending
- Final tag: pending
- Prior stable tag: `v0.9.7`
- Prior stable SHA: `bdfbaf2daed2c6ed0f0dd5527092171d91f1ac87`
- Prior release assets: [GitHub v0.9.7 release](https://github.com/FreeTAKTeam/LXMF-rs/releases/tag/v0.9.7)

## Scope review

The candidate is limited to the post-v0.9.7 fixes already present on
`origin/main`: resource-transfer request windows and scheduling, negotiated MTU
sizing, propagation bounds and failure reporting, SDK deadline enforcement,
lock-scope preparation, split-resource metadata, cancellation handling, and
compression. No new parity implementation is included in the release work.

The maintained parity boundary is unchanged: seven LXMF module rows are
complete for named software scenarios, while the RNS 1.4.2 inventory remains
1,695 complete, 115 partial, and 1 not applicable.

## Local gates

| Gate | Result | Evidence |
| --- | --- | --- |
| `cargo fmt --all -- --check` | PASS | `target/release-v0.9.8/gates/cargo-fmt.log` |
| `cargo clippy --workspace --all-targets --all-features --no-deps -- -D warnings` | PASS | `target/release-v0.9.8/gates/cargo-clippy.log` |
| `cargo check --workspace --all-targets` | PASS | `target/release-v0.9.8/gates/cargo-check.log` |
| `cargo nextest run --workspace --lib --bins` | PASS | `target/release-v0.9.8/gates/cargo-nextest.log` |
| `cargo test --workspace --tests` | PASS (clean rerun) | `target/release-v0.9.8/gates/cargo-test-workspace-tests-clean.log` |
| `cargo run -p xtask -- architecture-checks` | PASS | `target/release-v0.9.8/gates/architecture-checks.log` |
| `cargo run -p rns-tools --bin rnx -- e2e --timeout-secs 20` | PASS | `target/release-v0.9.8/gates/rnx-e2e.log` |
| `cargo xtask ci --stage sdk-schema-check` | PASS | `target/release-v0.9.8/gates/sdk-schema-check.log` |
| `cargo xtask publish-crates --wave all --dry-run --allow-dirty` | PASS (expected local-version fallback) | `target/release-v0.9.8/gates/publish-crates-dry-run.log` |
| `cargo xtask release-check` | PASS | `target/release-v0.9.8/gates/cargo-release-check.log`; generated scorecard, canary, reproducible-build, footprint, architecture, and module-size reports |

## Hosted and interop gates

- Candidate branch/commit: `codex/release-v0.9.8-execution` / `ba816e02f1af7650cbc19a1446d4486945dba690` (candidate ref update pending)
- Required CI run: [31120646834](https://github.com/FreeTAKTeam/LXMF-rs/actions/runs/31120646834) — contracts setup failed before repository steps; the other jobs were cancelled by the failed run.
- Release scorecard and leader readiness: PASS locally via `target/release-scorecard/` and `target/release-readiness/`; hosted promotion remains blocked.
- Pinned Python interop workflow: [31120646815](https://github.com/FreeTAKTeam/LXMF-rs/actions/runs/31120646815) — the original `0c494a53` candidate failed in `Rust/Python Reticulum channel interop`.
- The failure was a stale threshold in the three resource-backed request tests:
  negotiated TCP/Backbone MDUs made the 906-byte payload a normal packet, while
  the tests waited only for resource events. The transport implementation was
  not reverted or weakened.
- Interop blocker fix: `ba816e02f1af7650cbc19a1446d4486945dba690` sizes the
  test payload above the negotiated MDU and verifies request/response resource
  flags and correlation IDs.
- Local fixed result: all 24 channel cases pass, and the three repaired cases
  pass in three consecutive rounds. Evidence is retained in
  `target/release-v0.9.8/gates/python-channel-matrix-fixed.log` and the
  `*-fixed-round-*.log` files.
- A rerun of the old workflow was cancelled before executing repository steps;
  a fresh exact-SHA hosted dispatch is still required.
- Approved deferred cases: the boundary-mode recursive path-request case and
  the named Python-to-Rust remote-relay case remain explicit exceptions only if
  the corresponding workflow reports the same documented boundary behavior.

## RC and final publication

- `v0.9.8-rc.1`: BLOCKED pending repair and rerun of the resource-backed Python interop failures
- `origin/release` fast-forward: pending
- `v0.9.8` tag: pending
- GitHub release and assets/checksums: pending
- crates.io package versions: pending

## Known risks and rollback

- `RNS/Resource.py` remains partial because the Rust sender lacks the Python
  `receiver_min_consecutive_height` serving window.
- Hosted exact-SHA Python interop remains pending after the local blocker fix;
  no production transport failure remains reproduced locally.
- Physical radio/BLE/serial devices, public I2P, public networks, and
  third-party client claims remain outside this software release gate.
- Rollback is to restore `release` and the GitHub release/tag to the retained
  v0.9.7 SHA, then yank only packages proven to have been published in error;
  package rollback never rewrites crates.io history.
