# v0.9.9-rc.6 Release Candidate Evidence Ledger

Status: **HISTORICAL rc.6 RELEASE RECORD - release, CI, Verify, and leader readiness passed; the separate performance publication failed after measurements completed**

This ledger is the release record for the RNS 1.4.2 software-parity candidate.
It records the exact immutable tag, local and hosted gates, published artifacts,
provenance, and evidence boundaries. A pending or unavailable optional check is
recorded explicitly rather than silently converted to PASS.

## Candidate identity

| Field | Value |
|---|---|
| Candidate tag | `v0.9.9-rc.6` |
| Release commit | `61b3ccbdce34b2651dc9182c31a4ba81f6f76d0f` |
| Workspace/package version | `0.9.9` |
| Current branch base | `main` |
| Prior stable | `v0.9.8` |
| RNS reference | `1.4.2` at `b48b96e61676504e0a4e527b33b9a0b4495c6872` |
| LXMF reference | `727830cefda83d9c6e3982b48675425f3f988f9c` |
| GitHub release | [v0.9.9-rc.6](https://github.com/FreeTAKTeam/LXMF-rs/releases/tag/v0.9.9-rc.6) |
| Release notes | [`docs/release-notes-v0.9.9-rc.6.md`](../release-notes-v0.9.9-rc.6.md) |

## Parity target

| Total | Applicable | Complete | Partial | Unmapped | Not applicable |
|---:|---:|---:|---:|---:|---:|
| 1,811 | 1,810 | 1,810 | 0 | 0 | 1 |

The generated source of truth is
[`docs/status/python-surface-parity.json`](python-surface-parity.json). The
single not-applicable entry is the provenance-backed absent `CRNS` package.
The seven tracked LXMF module rows are complete for their named software
scenarios. Hardware, public-network, and third-party-client validation are
independent evidence axes.

## Documentation and metadata reconciliation

| Area | Result |
|---|---|
| `VERSION` and publishable workspace packages | PASS - aligned to `0.9.9`; private `test-support`/`xtask` package versions remain `0.1.0` by convention |
| RNS row-level matrix | PASS - software rows are complete; evidence boundaries are explicit |
| LXMF row-level matrix | PASS - all seven tracked rows remain complete |
| Current roadmap | PASS - `v0.9.9-rc.6` is current; v0.9.8 is historical |
| Release workflow docs/template | PASS - RC naming, MSI numeric version, stable-only channels, signing limits, and artifact names reconciled |
| Historical v0.9.8 records | PASS - retained without rewriting their release-boundary counts |

## Local validation

The broad local release-preparation gates passed on commit
`8801d963e7294cff6026072389c6f5d3fb02c031`. The rc2-rc6 changes were limited
to release dependencies, workflow/package fixes, and evidence baselines. The
current-tree check below reached the Windows-specific schema target-hash gate;
the hosted Linux leader gate is the authoritative result for that baseline.

| Gate | Result | Evidence |
|---|---|---|
| Python inventory self-test | PASS | `python tools/scripts/python_surface_inventory.py --self-test`; exact release target validated |
| Python inventory exact target | PASS | `--check` result: 1,811 total / 1,810 complete / 0 partial / 0 unmapped / 1 not applicable |
| Explicit pinned-reference inventory | PASS | Hosted exact-SHA Verify and Leader Readiness gates used RNS `b48b96e...` and LXMF `727830c...` |
| `cargo fmt --all -- --check` | PASS | Release-preparation validation on `8801d963...`; hosted CI also passed |
| `cargo check --workspace --all-targets` | PASS | Release-preparation validation on `8801d963...`; hosted CI and Release builds passed |
| `cargo test --workspace --tests` | PASS | Release-preparation validation; three Python relay tests were explicitly ignored because local reference repos/runtime were absent |
| `cargo clippy --workspace --all-targets --all-features --no-deps -- -D warnings` | PASS | Release-preparation validation on `8801d963...`; hosted CI passed |
| `cargo doc --workspace --no-deps` | PASS | Current rc6 release-check reached and passed documentation; one known private intra-doc-link warning remained non-fatal |
| `tools/scripts/check-boundaries.sh` | PASS | Release-preparation validation; hosted CI passed |
| `cargo run -p xtask -- architecture-checks` | PASS | Release-preparation validation; hosted CI passed |
| `cargo xtask ci` | PASS | Hosted CI run `31416209266` passed on the exact rc6 SHA |
| `cargo xtask release-check` | ENVIRONMENT-SPECIFIC BLOCK | Current Windows run reached the schema-client check but local generator hashes were `go 66f5...`, `javascript 7dda...`, `python 6b03...`; the hosted Linux baseline is `go d18...`, `javascript 1e05...`, `python 1b011...`, and the exact hosted Leader Readiness run passed |
| performance dashboard/check scripts | FAILED AT PUBLICATION | Run `31416212516` completed baseline, candidate, SDK, and E2E measurement stages, then rejected a 10.13% relative MAD under the former hard 10% cutoff. This branch classifies >10%-20% as an explicit warning and reserves hard failure for >20%; no rc.6 performance asset was published. |

## Hosted workflows

| Workflow | Run ID / URL | Result |
|---|---|---|
| CI | [`31416209266`](https://github.com/FreeTAKTeam/LXMF-rs/actions/runs/31416209266) | PASS on `61b3ccbdce34b2651dc9182c31a4ba81f6f76d0f` |
| Verify / HIL PR level | [`31416209274`](https://github.com/FreeTAKTeam/LXMF-rs/actions/runs/31416209274) | PASS on the exact rc6 SHA |
| Release | [`31416212210`](https://github.com/FreeTAKTeam/LXMF-rs/actions/runs/31416212210) | PASS; prerelease published with 20 assets |
| Performance release | [`31416212516`](https://github.com/FreeTAKTeam/LXMF-rs/actions/runs/31416212516) | FAILED during publication after all measurement stages; no performance asset was published |
| Leader readiness | [`31416212118`](https://github.com/FreeTAKTeam/LXMF-rs/actions/runs/31416212118) | PASS; all leader-grade readiness checks completed successfully |

The repository-owned HIL controller was read by scenario in the hosted
verification path. Its evidence boundaries still include the reference-only
boundary case and Python-to-Rust remote-relay case when those steps remain
deselected or skipped.

## Expected release assets

The release contains 20 assets. Every expected release class is present:

- Linux archives: `lxmf-rs_0.9.9-rc.6_linux-x86_64.tar.gz`,
  `lxmf-rs_0.9.9-rc.6_linux-aarch64.tar.gz`, and
  `lxmf-rs_0.9.9-rc.6_linux-armv7.tar.gz`.
- macOS archives: `lxmf-rs_0.9.9-rc.6_macos-x86_64.tar.gz`,
  `lxmf-rs_0.9.9-rc.6_macos-aarch64.tar.gz`, and
  `lxmf-rs_0.9.9-rc.6_macos-universal.tar.gz`.
- Windows packages: `lxmf-rs_0.9.9-rc.6_windows-x86_64.zip` and
  `lxmf-rs_0.9.9-rc.6_x64.msi`.
- Debian/RPM packages: `amd64`, `arm64`, and `armhf` `.deb` and `.rpm`
  assets, each named `lxmf-rs_0.9.9-rc.6_<arch>.<ext>`.
- SBOMs: `sbom-container.cdx.json`, `sbom-lxmf-cli.cdx.json`,
  `sbom-reticulumd.cdx.json`, and `sbom-rns-tools.cdx.json`.
- Checksums: `SHA256SUMS.txt` and `SHA256SUMS.txt.cosign.bundle`.

Verification completed against the downloaded release assets: 18 checksum
entries, 4/4 SBOM JSON files, 6/6 tar archives, the Windows ZIP, and the MSI
decompiled successfully with four executable references.

## Signing and provenance

| Surface | Result |
|---|---|
| Windows/Azure Trusted Signing | UNSIGNED - the release job recorded that Azure Trusted Signing secrets were not configured |
| macOS codesigning | UNSIGNED - the release job recorded that Apple signing secrets were not configured; notarization is not wired |
| Release checksum cosign bundle | PASS - bundle verified |
| GitHub release-file attestations | PASS - 13/13 release files verified, plus the checksum bundle |
| OCI cosign signature | PASS - `gh attestation verify oci://ghcr.io/freetakteam/lxmf-rs:0.9.9-rc.6 --owner FreeTAKTeam` |
| OCI build-provenance attestation | PASS - verified for the published version image |

The eight release archives, four SBOMs, and `SHA256SUMS.txt` all passed GitHub
attestation verification.

## OCI publication

| Field | Result |
|---|---|
| Version tag | `ghcr.io/freetakteam/lxmf-rs:0.9.9-rc.6` - published and verified |
| Stable `latest` tag | PASS - unchanged; `latest` returned `manifest unknown` |
| Platforms | PASS - `linux/amd64` (`sha256:8b66c60e118cea4b050ec87a72e7e37472b9b3b68b1c0f725837e678144217aa`) and `linux/arm64` (`sha256:c288490079a93ebf95fadadbeabb5d87ddbd196ce8e7af74104b8e9c63a22464`) |
| SBOM | PASS - container SBOM asset published and verified |
| Signature and attestation | PASS |

## Performance dashboard

| Field | Result |
|---|---|
| Workflow run | [`31416212516`](https://github.com/FreeTAKTeam/LXMF-rs/actions/runs/31416212516), FAILED during publication after measurement completed |
| JSON asset | NOT PUBLISHED for rc.6 |
| HTML asset | NOT PUBLISHED for rc.6 |
| Dashboard checksum | NOT PUBLISHED for rc.6 |
| Numeric interpretation | No rc.6 performance numbers are claimed because the workflow did not publish a validated dataset |

Post-rc.6 independent implementation and replacement performance evidence is
tracked by the current roadmap, `docs/interop`, and
[`docs/performance/v0.9.9.json`](../performance/v0.9.9.json). Exact-SHA workflow
[`31550356625`](https://github.com/FreeTAKTeam/LXMF-rs/actions/runs/31550356625)
passed the five-sample regression gate without warnings and published
checksummed JSON, HTML, and raw benchmark evidence as an Actions artifact. This
does not rewrite the immutable rc.6 release boundary or attach evidence from a
different commit to the rc.6 release; release assets remain tag-triggered.

## Deferred evidence boundaries

These are explicitly deferred and do not downgrade the software inventory:

- physical RNode, RNodeMulti, Weave, VR-N76, BLE, and serial-radio validation;
- public I2P and public Reticulum network operation/soak;
- Sideband, MeshChatX, Columba, and other third-party-client compatibility;
- the pinned workflow's reference-only boundary case and Python-to-Rust
  remote-relay scenario when they are not executed.

## Recommendation

**READY FOR PRERELEASE DISTRIBUTION** - `v0.9.9-rc.6` is published from the
exact candidate SHA with the strict RNS 1.4.2 software-parity target, passing
CI, Verify, Release, and Leader Readiness evidence, verified artifacts,
attestations, and OCI publication. The rc.6 performance publication itself
remains failed and immutable; a later exact-SHA five-sample replacement run is
now PASS and is recorded above. Final stable `v0.9.9` publication remains
outside this candidate ledger and requires the tag workflows to attach evidence
for the final tag commit.
