syntax = "proto3";

package lxmf.common.v1;

// Standard opaque page token request used by paginated list calls.
message PageRequest {
  string page_token = 1;
  uint32 page_size = 2;
}

// Continuation metadata returned by paginated list calls.
message PageInfo {
  string next_page_token = 1;
}
