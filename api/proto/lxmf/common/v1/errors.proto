syntax = "proto3";

package lxmf.common.v1;

// Normalized machine-readable error details for cross-service responses.
message ErrorDetail {
  string code = 1;
  string message = 2;
  string category = 3;
  bool retryable = 4;
  bool user_actionable = 5;
}
